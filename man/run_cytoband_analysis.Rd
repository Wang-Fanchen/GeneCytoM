% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_analysis.R
\name{run_cytoband_analysis}
\alias{run_cytoband_analysis}
\title{Run Complete Cytoband Analysis Pipeline}
\usage{
run_cytoband_analysis(
  input_file,
  output_dir = "results",
  comparison_filter = "up",
  pvalue_threshold_g = NULL,
  pvalue_threshold_m = NULL,
  top_n = 20,
  create_plots = TRUE,
  save_intermediate = TRUE
)
}
\arguments{
\item{input_file}{Character string specifying path to input CSV file}

\item{output_dir}{Character string specifying output directory (default: "results")}

\item{comparison_filter}{Character string for candidate gene filtering:
"up", "down", "mid", or "all" (default: "up")}

\item{pvalue_threshold_g}{Numeric p-value threshold for P.Value.G filtering (default: NULL).
If NULL or if P.Value.G column doesn't exist, this filter is skipped.}

\item{pvalue_threshold_m}{Numeric p-value threshold for P.Value.M filtering (default: NULL).
If NULL or if P.Value.M column doesn't exist, this filter is skipped.}

\item{top_n}{Numeric number of top cytobands for visualization (default: 20)}

\item{create_plots}{Logical whether to create visualization plots (default: TRUE)}

\item{save_intermediate}{Logical whether to save intermediate results (default: TRUE)}
}
\value{
A list containing analysis results:
\itemize{
\item processed_data: Full processed gene data with location information
\item candidate_genes: Filtered candidate genes
\item cytoband_summary: Cytoband-level attribution summary
\item comprehensive_summary: 5-column comprehensive summary
\item plots: List of plot objects (if create_plots = TRUE)
}
}
\description{
Executes the complete cytoband analysis pipeline from data processing to visualization.
This is the main function that integrates all analysis steps.
}
\details{
This function executes the complete analysis pipeline:
\enumerate{
\item Data processing and location matching
\item Candidate gene screening
\item Cytoband attribution analysis
\item Comprehensive summary generation
\item Visualization (ideograms, line plots, lollipop charts)
\item Automatic file saving
}

The input CSV file must contain these required columns:
Gene, Local.Index.G, Local.Index.M, Attribution.G, Attribution.M

Optional columns:
P.Value.G, P.Value.M
}
\examples{
\dontrun{
# Run complete analysis with default parameters
results <- run_cytoband_analysis(
  input_file = "gene_data.csv",
  output_dir = "analysis_results"
)

# Run analysis with custom parameters
results <- run_cytoband_analysis(
  input_file = "my_data.csv",
  output_dir = "custom_results",
  comparison_filter = "all",
  pvalue_threshold_g = 0.05,
  pvalue_threshold_m = 0.01,
  top_n = 30
)

# Run without P-value filtering
results <- run_cytoband_analysis(
  input_file = "my_data.csv",
  output_dir = "custom_results",
  comparison_filter = "up"
)

# Access specific results
candidate_count <- nrow(results$candidate_genes)
top_cytobands <- head(results$cytoband_summary, 10)
}

}
